namespace Melodix.Modelos
{
        public enum RolUsuario { Usuario, Musico, Admin }
        public enum TipoObjetoLike { Pista, Album, Lista }
        public enum AccionLike { Like, Unlike }
        public enum EstadoSuscripcion { Activa, Cancelada, Pendiente, Vencida }
}